dispatch:
  # d service serves the typical web resources and all static resources.
  - url: "*/process"
    service: default
  - url: "*/"
    service: default
  - url: "*/users"
    service: default
